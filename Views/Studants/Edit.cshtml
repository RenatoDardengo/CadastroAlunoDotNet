@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h3 class="display-4">@ViewData["Titulo"]</h3>

</div>
<div>
    
    <form method="put" action="/edit/@ViewBag.Id" onsubmit=" return confirm('Confirmar o envio dos dados?')">
        <div class="form-group">
            <div class="row mt-5">
                <div class="col">
                    <label for="nome">Nome</label>
                    <input type="text" value="@ViewBag.Nome" class="form-control" id="nome" name="nome" aria-describedby="emailHelp">
                </div>
                <div class="col">
                    <label for="matricula">Matrícula</label>
                    <input type="text" value="@ViewBag.Matricula"class="form-control" id="matricula" name="matricula">
                </div>

            </div>

        </div>
        <div class="form-group">
            <div class="row mt-5">
                <div class="col">
                    <label for="notas"> Primeira Nota</label>
                    <input type="text" value="@ViewBag.nota" class="form-control" id="notas" name="notas">
                </div>
                
            </div>

        </div>

        <button type="submit" class="btn btn-primary mt-5">Cadastrar</button>
    </form>
</div>
